
command + n 新建图层
背景是不能移动的 图层才能移动
shift 可以控制移动在垂直 或者水平线上
上下左右可以控制移动像素 每次按一下移动一像素
x是恢复到黑白颜色 d是切换前景色和背景色的颜色
command+d是取消矩形选择工具
shift+alt 保持圆心点不变花正方形
shift 画正方形

居中 是针对多个图层的

新建图层:shift alt command n

保存 command + s
默认的格式:psd(源文件格式)

打开有两种方式 command+o 或者直接拖拽进去.
jpg是压缩格式 只有一个图层

tab 隐藏面板
tab + shift 隐藏右边面板

command + 放大

空格键 抓手工具 
按回车确定

常规可以设置 command+k 一些shift一类的快捷键

command+t 自由变换

command+g 新建组

alt是固定中点的 shift键是保持形状不变的

command 点击图层可以载入选区

羽化 ==  移动工具 -> 选择 -> 修改 -> 语法

复位 == 窗口 -> 工作区 -> 复位基本功能

掏空用选区然后按delete


所有的透明都是用网格表示的 解开了我之前的一大困扰
注意 不是每种格式都支持透明 比如jpg 它不支持透明 它会自动变成白底
反选 command+shift +i

delete 套索工具上一部

频率默认是57 数值越大 节点越多  越不平滑 主要是针对 不好选的区域时候选择数值大的

alt 是减法运算 shift加饭运算 alt+shift 是乘法运算

先按shift是运算  先画再按shift是画圆

容差表示可识别的颜色范围 越大 识别的相近颜色越多 默认是32

